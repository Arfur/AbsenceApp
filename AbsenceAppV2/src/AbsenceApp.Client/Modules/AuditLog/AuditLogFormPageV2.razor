@* ===========================================================================
   File        : AuditLogFormPageV2.razor
   Namespace   : AbsenceApp.Client.ModulesV2.AuditLog
   Author      : Michael
   Version     : 1.2.1
   Created     : 2026-03-21
   Updated     : 2026-04-04
   ---------------------------------------------------------------------------
   Purpose     : V2 Audit Log view page ("form" stub). Read-only view displaying
                 a single audit log entry with a notice that entries are
                 immutable. Reuses AuditLogDetailViewModelV2.
   ---------------------------------------------------------------------------
   Changes     :
     - 1.0.0  2026-03-21  Initial implementation (Phase 7).
     - 1.0.1  2026-03-25  Removed invalid @using Components.PageTemplatesV2 and/or
                          Components.TableV2. These components are provided via
                          the correct Framework namespaces through Modules/_Imports.razor.
     - 1.1.0  2026-03-26  Replaced fully-qualified layout
                          'AbsenceApp.Client.Components.Layout.MainLayoutV2'
                          with 'MainLayoutV2' to remove dependency on the
                          obsolete Components.Layout namespace. Added explicit
                          @using for Framework page templates so DetailPageV2
                          and DetailSectionV2 resolve correctly. Normalised UI
                          text encoding. No functional changes.
     - 1.2.0  2026-03-26  Replaced @layout MainLayoutV2 with @layout MainLayoutV2;
                          MainLayoutV2 no longer exists — BaseLayoutV2 is the
                          V2 authenticated-shell layout (CS0246 build error fix).
     - (bumped)  2026-04-04  Phase 3 Stabilisation Issue 2: changed @layout from BaseLayoutV2 to MainLayoutV2 so all module pages use the canonical V2 shell layout with correct fixed-header padding.
   ---------------------------------------------------------------------------
   Notes       :
     - Route: /v2/auditlog/{Id:long}/view
     - Register AuditLogDetailViewModelV2 as Scoped in Phase 10.
   ===========================================================================
*@

@page "/v2/auditlog/{Id:long}/view"

@using AbsenceApp.Client.Framework.PageTemplates
@using AbsenceApp.Client.ViewModels.V2

@layout MainLayoutV2
@inject AuditLogDetailViewModelV2 ViewModel
@inject AppStateService AppState

@* =========================================================================
   Section: Audit log form
   ========================================================================= *@
<PageTemplateV2 PageTitleText="@PageTitle">

    <ActionsContent>
        <a href="/v2/auditlog" class="dsv2-btn dsv2-btn--secondary dsv2-btn--sm">
            <i class="bi bi-arrow-left" aria-hidden="true"></i> Audit Log
        </a>
    </ActionsContent>

    <ChildContent>
        <div class="dsv2-alert dsv2-alert--info" role="note">
            <i class="bi bi-lock" aria-hidden="true"></i>
            Audit logs are read-only records. They cannot be created or modified.
        </div>

        @if (ViewModel.IsLoading)
        {
            <p class="dsv2-loading-text">Loading...</p>
        }
        else if (ViewModel.Error is not null)
        {
            <div class="dsv2-alert dsv2-alert--danger" role="alert">@ViewModel.Error</div>
        }
        else if (ViewModel.Item is not null)
        {
            var entry = ViewModel.Item;

            <section class="dsv2-detail-section">
                <h2 class="dsv2-detail-section__title">
                    <i class="bi bi-shield-check" aria-hidden="true"></i>
                    Audit Entry
                </h2>
                <div class="dsv2-detail-grid">
                    <span class="dsv2-detail-label">Audit ID</span>
                    <span class="dsv2-detail-value">@entry.AuditId</span>

                    <span class="dsv2-detail-label">User ID</span>
                    <span class="dsv2-detail-value">@entry.UserId</span>

                    <span class="dsv2-detail-label">Action</span>
                    <span class="dsv2-detail-value">@entry.Action</span>

                    <span class="dsv2-detail-label">Timestamp</span>
                    <span class="dsv2-detail-value">@entry.Timestamp.ToString("dd MMM yyyy HH:mm:ss")</span>
                </div>
            </section>
        }
    </ChildContent>

</PageTemplateV2>

@* =========================================================================
   Section: ViewModel wiring and form submission
   ========================================================================= *@
@code {
    [Parameter] public long Id { get; set; }

    private string PageTitle => ViewModel.Item is not null
        ? $"View Audit Entry #{ViewModel.Item.AuditId}"
        : "View Audit Entry";

    protected override async Task OnParametersSetAsync()
    {
        AppState.SetBreadcrumb("Audit Log", "View Entry");
        await ViewModel.LoadAsync(Id);
    }
}
