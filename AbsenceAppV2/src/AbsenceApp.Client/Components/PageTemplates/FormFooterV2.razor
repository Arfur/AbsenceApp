@* ===========================================================================
   File        : FormFooterV2.razor
   Namespace   : AbsenceApp.Client.Components.PageTemplatesV2
   Author      : Michael
   Version     : 1.1.0
   Created     : 2026-03-21
   Updated     : 2026-03-23
   ---------------------------------------------------------------------------
   Purpose     : Sticky footer bar for FormPageV2. Contains a primary submit
                 button and a secondary cancel button. Supports a loading/busy
                 state on the submit button and renders optional BodyContent
                 for extra controls (e.g. Delete button on edit forms).
   ---------------------------------------------------------------------------
   Changes     :
     - 1.0.0  2026-03-21  Initial implementation.
     - 1.1.0  2026-03-23  Header updated for Step 3 (no layout binding required).
   ---------------------------------------------------------------------------
   Notes       :
     - Phase 5 component. No DI registration required.
     - Not a page template — does not inherit BaseLayoutV2.
   ===========================================================================
*@

@* =========================================================================
   Section: Form footer
   ========================================================================= *@
<footer class="dsv2-form-footer">
    <div class="dsv2-form-footer__start">
        @BodyContent
    </div>
    <div class="dsv2-form-footer__end">
        @if (OnCancel.HasDelegate)
        {
            <button type="button"
                    class="dsv2-form-footer__btn dsv2-form-footer__btn--cancel"
                    disabled="@IsBusy"
                    @onclick="OnCancel">
                <i class="bi bi-x-lg" aria-hidden="true"></i>
                @CancelLabel
            </button>
        }
        <button type="submit"
                class="@SubmitBtnClass()"
                disabled="@(IsBusy || IsDisabled)"
                @onclick="OnSubmit">
            @if (IsBusy)
            {
                <span class="dsv2-form-footer__spinner" role="status"
                      aria-label="Saving…"></span>
                <span>@BusyLabel</span>
            }
            else
            {
                <i class="bi bi-check-lg" aria-hidden="true"></i>
                <span>@SubmitLabel</span>
            }
        </button>
    </div>
</footer>

@* =========================================================================
   Section: Parameters and busy state
   ========================================================================= *@
@code {
    // -------------------------------------------------------------------------
    // Parameters
    // -------------------------------------------------------------------------
    [Parameter] public string SubmitLabel { get; set; } = "Save";
    [Parameter] public string BusyLabel { get; set; } = "Saving\u2026";
    [Parameter] public string CancelLabel { get; set; } = "Cancel";
    [Parameter] public bool IsBusy { get; set; } = false;
    [Parameter] public bool IsDisabled { get; set; } = false;
    [Parameter] public EventCallback OnSubmit { get; set; }
    [Parameter] public EventCallback OnCancel { get; set; }
    [Parameter] public RenderFragment? BodyContent { get; set; }

    // -------------------------------------------------------------------------
    // Helpers
    // -------------------------------------------------------------------------
    private string SubmitBtnClass()
    {
        var cls = "dsv2-form-footer__btn dsv2-form-footer__btn--submit";
        if (IsBusy) cls += " dsv2-form-footer__btn--busy";
        return cls;
    }
}
