Things to do:
1. When the login page appear to should move the focus to the 'username' input box so you can start to type immediately.
2. In Global Settings when you click Templates > 'Pages' or 'Layouts' it takes you back to the login screen
3. Clicke staff details login screen



1. Dashboard > Attendance & Trends 'Page not found'
2. PEOPLE > Students > Student Details 'Page not found'
3. ACADEMICS > Classes > Class Details 'Page not found'
4. SETTINGS > Diagnostics 'Page not found'
5. SETTINGS > Site 'Page not found'

Global Settings
1. Icons
2. Spacing
3. Templates > Layouts
4. Templates > Pages

May need to do this at a later date:
Move the Table template files to its own folder under:
C:\DevAbsence2\AbsenceAppV2\src\AbsenceApp.Client\Shared\Templates\Tables
Shared/Templates/Tables/
    TablePageTemplateV2.razor
    TablePageTemplateV2.razor.css

The CSS for buttons is in app.css and really should be in buttons.css


Need to look at the Index.razor for Buttons because it has repeated code for every button group:
        @* ───────────────────────────────────────────────────────────────
            Header: SECTION
        ─────────────────────────────────────────────────────────────── *@
        <UiKitsGroup Id="btn-group-icon"
                     Title="@_iconState.GroupTitle"
                     IsAccordionOpen="@_iconState.AccordionOpen"
                     IsAccordionOpenChanged="EventCallback.Factory.Create<bool>(this, _ => OnToggleAccordion(_iconState))"
                     Items="@(_iconState.Variants.Select(v => new UiKitsGroup.UiKitsDemoItem(v.Key, v.Label, _iconRenderClassByKey[v.Key], _iconClassByKey[v.Key])).ToList())"
                     OnItemSelectedKeyChanged="EventCallback.Factory.Create<string>(this, key => OnSelectedVariantChanged(_iconState, key))"
                     IsEditing="@_iconState.IsEditing"
                     OnEdit="EventCallback.Factory.Create(this, () => OnEditSaveClickedAsync(_iconState))"
                     EditorText="@_iconState.EditorText"
                     EditorTextChanged="EventCallback.Factory.Create<string>(this, v => OnEditorInput(_iconState, v))"
                     IsSaving="@_iconState.IsSaving"
                     OnSave="EventCallback.Factory.Create(this, () => OnEditSaveClickedAsync(_iconState))"
                     OnCancel="EventCallback.Factory.Create(this, () => OnCancelClicked(_iconState))"
                     PreviewCss="@_iconState.PreviewCss"
                     StatusMessage="@_iconState.StatusMessage"
                     StatusIsError="@_iconState.StatusIsError">
            <ItemTemplate Context="ctx">
                <button type="button" class="@ctx.Item.CssClass" aria-label="@ctx.Item.Label">
                    <i class="@ctx.Item.IconClass"></i>
                </button>
            </ItemTemplate>
        </UiKitsGroup>
		
		
Need to sort the LIGHT and DARK theme in 'Theme.json' at the moment they are the same.