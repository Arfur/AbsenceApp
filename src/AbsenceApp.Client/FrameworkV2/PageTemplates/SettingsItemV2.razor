@* ===========================================================================
   File        : SettingsItemV2.razor
   Namespace   : AbsenceApp.Client.Components.PageTemplatesV2
   Author      : Michael
   Version     : 1.0.0
   Created     : 2026-03-21
   Updated     : 2026-03-21
   ---------------------------------------------------------------------------
   Purpose     : Single settings row rendered inside a SettingsGroupV2. Left side: icon, label, description. Right side: ChildContent (toggle, dropdown, input, etc.) or HintText. Accepts a SettingsItemModel for configuration or inline parameters.
   ---------------------------------------------------------------------------
   Changes     :
     - 1.0.0  2026-03-21  Initial implementation.
   ---------------------------------------------------------------------------
   Notes       :
     - Phase 5 component. No DI registration required.
   ===========================================================================
*@
@using AbsenceApp.Client.Models.V2

@* =========================================================================
   Section: Settings item
   ========================================================================= *@
<div class="@ItemClass()" role="listitem">
    <div class="dsv2-settings-item__label-col">
        @if (Icon is not null)
        {
            <i class="bi @Icon dsv2-settings-item__icon" aria-hidden="true"></i>
        }
        <div class="dsv2-settings-item__text">
            <span class="dsv2-settings-item__label">
                @Label
                @if (!string.IsNullOrEmpty(BadgeText))
                {
                    <span class="dsv2-settings-item__badge">@BadgeText</span>
                }
            </span>
            @if (!string.IsNullOrEmpty(Description))
            {
                <span class="dsv2-settings-item__description">@Description</span>
            }
        </div>
    </div>

    <div class="dsv2-settings-item__control-col">
        @if (ChildContent is not null)
        {
            @ChildContent
        }
        else if (!string.IsNullOrEmpty(HintText))
        {
            <span class="dsv2-settings-item__hint">@HintText</span>
        }
    </div>
</div>

@* =========================================================================
   Section: Parameters
   ========================================================================= *@
@code {
    // -------------------------------------------------------------------------
    // Parameters
    // -------------------------------------------------------------------------
    /// <summary>Optional model. When supplied its properties override defaults.</summary>
    [Parameter] public SettingsItemModel? Model { get; set; }

    /// <summary>Primary label text for the settings item.</summary>
    [Parameter] public string Label { get; set; } = string.Empty;

    /// <summary>Optional supporting description below the label.</summary>
    [Parameter] public string? Description { get; set; }

    /// <summary>Optional hint text shown in the control area when no ChildContent is provided.</summary>
    [Parameter] public string? HintText { get; set; }

    /// <summary>Bootstrap icon class beside the label (e.g. "bi-moon").</summary>
    [Parameter] public string? Icon { get; set; }

    /// <summary>When true the row renders with reduced opacity and pointer-events disabled.</summary>
    [Parameter] public bool Disabled { get; set; } = false;

    /// <summary>Optional badge text (e.g. "Beta", "New") shown adjacent to the label.</summary>
    [Parameter] public string? BadgeText { get; set; }

    /// <summary>The control element (toggle, select, input, etc.) for this settings item.</summary>
    [Parameter] public RenderFragment? ChildContent { get; set; }

    // -------------------------------------------------------------------------

    // Lifecycle

    // -------------------------------------------------------------------------
    protected override void OnParametersSet()
    {
        if (Model is not null)
        {
            if (string.IsNullOrEmpty(Label)) Label = Model.Label;
            Description ??= Model.Description;
            HintText    ??= Model.HintText;
            Icon        ??= Model.Icon;
            BadgeText   ??= Model.BadgeText;
            if (!Disabled) Disabled = Model.Disabled;
        }
    }

    private string ItemClass()
    {
        var cls = "dsv2-settings-item";
        if (Disabled) cls += " dsv2-settings-item--disabled";
        return cls;
    }
}
