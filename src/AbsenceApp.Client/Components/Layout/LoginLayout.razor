@* ===========================================================================
   File        : LoginLayout.razor
   Namespace   : AbsenceApp.Client.Components.Layout
   Author      : Michael
   Version     : 1.0.0
   Created     : 2026-03-17
   Updated     : 2026-03-17
   ---------------------------------------------------------------------------
   Purpose     : Minimal layout applied to the Login and Register pages.
                 Renders only the auth-root shell without the app navbar,
                 sidebar, or footer, keeping the auth UI uncluttered.
   ---------------------------------------------------------------------------
   Notes       :
     - Applied via @layout LoginLayout in Login.razor and Register.razor.
     - auth-root styles (full-viewport, centred) are defined in app.css.
   ---------------------------------------------------------------------------
   Changes     :
     - 1.0.0  2026-03-17  Initial implementation.
   ===========================================================================
*@
@inherits LayoutComponentBase

@* =========================================================================
   Section: Auth shell — full-viewport wrapper for authentication pages
   ========================================================================= *@
<div class="auth-root">
    @Body
</div>
